export const baseStyles = `:root {
  color-scheme: light;
  font-family: Inter, system-ui, sans-serif;
}

*, *::before, *::after {
  box-sizing: border-box;
}`;

export const globalReset = `html, body {
  margin: 0;
  min-height: 100%;
}

body {
  background: #F8FAFC;
}`;
